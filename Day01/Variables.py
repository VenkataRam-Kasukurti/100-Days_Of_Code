# learn to store in container for later use. Variables is a concept in programming that allows us to give a lable to a piece of data so that we can refer or reference that data using the chosen variable name.

'''name = input("What is your name?")
print(name)
print(len(name))'''

# print(len(input("What is your name?")))

username = input("What is your name?")
length = len(username)
print(length)
